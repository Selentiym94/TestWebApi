
@{
    ViewBag.Title = "Удаление пользователя";
}

<div>
    <h3>Редактор</h3>
    @if (ViewBag.check == true)
    {
        if (ViewBag.Admin.Role == "admin")
        {
            <table>
                <tr><td>Удаление выполняется администратором: @ViewBag.Admin.Name</td>
                <tr><td>Пользователь @ViewBag.User.Name успешно удален</td>
                <tr><td>Все @ViewBag.UserdeviceCount устройтсва успешно удален(ы)</td>
                <tr><td><a href="/AdminInformation/Info/@ViewBag.Admin.Id">Назад</a></td></tr>
            </table>
        }
        else
        {
            <table>
                <tr><td>У пользователя: @ViewBag.Admin.Name (id=@ViewBag.Admin.Id) нет прав для совершения данной операции</td>
                <tr><td><a href="/AdminInformation/Info/@ViewBag.Admin.Id">Назад</a></td></tr>
            </table>
        }
    }
    else
    {
        <table>
            <tr><td>@ViewBag.mes</td>
            <tr><td><a href="/AdminInformation/Info/@ViewBag.Admin.Id">Назад</a></td></tr>
        </table>
    }
</div>